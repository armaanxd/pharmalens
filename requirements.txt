# PharmaLens Requirements
# Install with: pip install -r requirements.txt (after conda install rdkit vina)

# Core ML/AI
torch>=2.0.0
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0
sentencepiece>=0.1.99

# Chemistry & Bioinformatics
biopython>=1.81
# rdkit (install via conda: conda install -c conda-forge rdkit)
meeko>=0.4.0
# vina (install via conda: conda install -c conda-forge vina)

# Visualization
py3Dmol>=2.0.0
plotly>=5.14.0
matplotlib>=3.7.0

# UI Framework
gradio>=4.0.0

# Utilities
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Optional: Molecule generation models
# moleculekit (if using advanced generation)
# molvs (molecule validation/standardization)
